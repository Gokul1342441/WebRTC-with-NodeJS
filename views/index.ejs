<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Baloo Tamma 2', cursive;
            background-color: #0080AD;
        }

        #video_display {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        #video_container {
            display: flex;
            width: 80%;
        }

        .local_div,
        .remote_div {
            flex: 1;
            overflow: hidden;
            position: relative;
        }

        .remote_div {
            margin-left: 10px; /* Adjust the spacing between local and remote videos */
        }

        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .label {
            text-align: center;
            margin-top: 5px;
            color: #fff;
        }
    </style>
    <title>WebTutsPlus WebCon</title>
</head>

<body>
    <div id="video_display">
        <div id="video_container">
            <div class="local_div" id="div1">
                <p class="label">Primary Laptop Webcam</p>
                <!-- For playing primary laptop webcam -->
                <video id="localVideo" class="" autoplay muted playsinline></video>
            </div>
            <div class="remote_div" id="div2">
                <p class="label">Mobile Camera</p>
                <!-- For playing mobile camera -->
                <video id="remoteVideo" class="" autoplay playsinline></video>
            </div>
        </div>
    </div>

    <!-- Import SocketIO for signalling -->
    <script src="/socket.io/socket.io.js"></script>

    <!-- Import WebRTC adapter for compatibility with all the browsers  -->
    <script src="https://webrtc.github.io/adapter/adapter-latest.js"></script>

    <!-- Import TURN config -->
    <script src="js/config.js"></script>

    <!-- Import script containing WebRTC related functions -->
    <script src="js/main.js"></script>
</body>

</html>
